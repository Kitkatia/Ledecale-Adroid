<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cafe.decale.ledecale">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar"
        >
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConnectionActivity"
            android:label="@string/ConnectionActivity" />
        <activity
            android:name=".CalendarBookActivity"
            android:label="@string/CalendarBookActivity" />
        <activity
            android:name=".CheckAvailability"
            android:label="@string/CheckAvailability" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/RegisterActivity" />
        <activity
            android:name=".CategoryListActivity"
            android:label="@string/CategoryListActivity" />
        <activity
            android:name=".FindGameActivity"
            android:label="@string/FindActivityGame" />
        <activity
            android:name=".MyProfileActivity"
            android:label="@string/MyProfileActivity" />
        <activity
            android:name=".EditProfileActivity"
            android:label="@string/EditProfileActivity" />
        <activity
            android:name=".MyBookings"
            android:label="@string/MyBookings" />
        <activity
            android:name=".BookActivity"
            android:label="@string/BookActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/MainActivity" />
        <activity
            android:name=".GameListActivity"
            android:label="@string/title_game_list_activity" />
        <activity
            android:name=".NewsListActivity"
            android:label="@string/title_news_list_activity" />
        <activity
            android:name=".BookingListActivity"
            android:label="@string/title_event_list_activity" />
        <activity
            android:name=".GameActivity"
            android:label="@string/title_game_activity" />

    </application>

</manifest>